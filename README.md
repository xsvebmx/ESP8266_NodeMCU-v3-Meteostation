# 🌦️ ESP8266 NodeMCU v3 Meteostation: Полная документация

Документация к проекту простой метеостанции на базе **ESP8266 NodeMCU v3** с использованием датчика **BME280**. Эта метеостанция предназначена для измерения параметров окружающей среды и предоставляет возможность работы в двух режимах: локальном (через веб-интерфейс) и API-режиме (с отправкой данных на сервер Flask для построения графиков). В этом документе вы найдете все необходимое для сборки, настройки и использования метеостанции, включая устранение возможных неисправностей.

---

## Описание проекта

Метеостанция на базе **ESP8266 NodeMCU v3** и датчика **BME280** — это компактное устройство, которое собирает данные о погоде и предоставляет их пользователю. Проект поддерживает два режима работы:

- **Локальный веб-интерфейс**: данные отображаются непосредственно на веб-странице устройства.
- **API-режим**: данные отправляются на сервер Flask, где сохраняются и визуализируются в виде графиков.

---

## ⚙️ Возможности

### ESP8266 NodeMCU
- **Wi-Fi**: поддерживает режим точки доступа (AP) и подключение к существующей сети (STA).
- **Измеряемые параметры**:
  - Температура (°C)
  - Влажность воздуха (%)
  - Атмосферное давление (гПа и мм рт. ст.)
  - Высота над уровнем моря (м)
- **Настройка**: выбор режима работы и URL сервера через веб-интерфейс.
- **Хранение настроек**: использование EEPROM для сохранения параметров между перезапусками.

### Flask-сервер
- **Прием данных**: принимает данные через POST-запросы на endpoint `/data`.
- **Возврат данных**: отдает последние замеры через GET-запрос на `/data`.
- **Визуализация**: автоматически генерирует графики в формате PNG:
  - `/plot/temperature.png` — температура
  - `/plot/humidity.png` — влажность
  - `/plot/pressure.png` — давление в гПа
  - `/plot/pressure_mmHg.png` — давление в мм рт. ст.
  - `/plot/altitude.png` — высота

---

## 📋 Требования

### Оборудование
- **ESP8266 NodeMCU v3** (микроконтроллер с поддержкой Wi-Fi)
- **Датчик BME280** (I2C интерфейс для измерения температуры, влажности и давления)
- USB-кабель для прошивки и питания
- Компьютер для настройки и запуска сервера

### Программное обеспечение
- **Arduino IDE** или другая среда для прошивки ESP8266
- **Python 3.x** для запуска Flask-сервера
- Библиотеки Python:
  - `flask`
  - `pandas`
  - `matplotlib`
- Код проекта:
  - `weather_station.ino` (прошивка для ESP8266)
  - `server.py` (Flask-сервер)

---

## 📦 Структура проекта

```
ESP8266_NodeMCU-v3-Meteostation/
├── server.py           # Flask-сервер для обработки данных и построения графиков
├── data.jsonl          # Файл с историей замеров в формате JSON Lines
├── plots/              # Папка для хранения сгенерированных графиков
│   ├── temperature.png # График температуры
│   ├── humidity.png    # График влажности
│   ├── pressure.png    # График давления в гПа
│   ├── pressure_mmHg.png # График давления в мм рт. ст.
│   └── altitude.png    # График высоты
├── weather_station.ino # Код прошивки для ESP8266
└── README.md           # Документация проекта
```

---

## 🔌 Подключение оборудования

Датчик **BME280** подключается к **ESP8266 NodeMCU v3** через интерфейс I2C. Схема подключения:

| Пин BME280 | Пин ESP8266 NodeMCU v3 |
|------------|------------------------|
| VIN        | 3V3                   |
| GND        | GND                   |
| SDA        | D2 (GPIO4)            |
| SCL        | D1 (GPIO5)            |

**Примечание**: Адрес I2C датчика BME280 по умолчанию — `0x76`. Убедитесь, что ваш датчик использует этот адрес, либо измените его в коде, если он отличается (например, `0x77`).

---

## 🚀 Установка и настройка

### 1. Подключение и прошивка ESP8266
1. Подключите датчик BME280 к ESP8266 согласно схеме выше.
2. Загрузите файл `weather_station.ino` в Arduino IDE.
3. Установите необходимые библиотеки:
   - `Adafruit_BME280` (для работы с датчиком)
   - `ESP8266WiFi` (для Wi-Fi)
   - `ESP8266WebServer` (для веб-интерфейса)
   - `EEPROM` (для сохранения настроек)
4. Подключите ESP8266 к компьютеру через USB и выполните прошивку.

После первой загрузки устройство создаст точку доступа:
- **SSID**: `NodeConfig`
- **Пароль**: `12345678`

5. Подключитесь к этой точке доступа с телефона или компьютера.
6. Откройте браузер и перейдите по адресу: [http://192.168.4.1](http://192.168.4.1).
7. Настройте устройство:
   - Выберите режим работы:
     - `L` — локальный веб-интерфейс (данные доступны на странице `/data`)
     - `A` — API-режим (отправка данных на сервер)
   - Укажите SSID и пароль вашей Wi-Fi сети (для подключения к ней).
   - В API-режиме укажите URL сервера Flask (например, `http://192.168.1.100:5000/data`).

Настройки сохранятся в EEPROM, и устройство перезагрузится.

### 2. Запуск Flask-сервера
1. Убедитесь, что на компьютере установлен Python 3.x.
2. Установите необходимые библиотеки:
   ```bash
   pip install flask pandas matplotlib
   ```
3. Поместите файл `server.py` в папку проекта.
4. Запустите сервер:
   ```bash
   python server.py
   ```
5. Сервер будет доступен по адресу `http://localhost:5000` (или IP вашего компьютера в локальной сети).

Сервер автоматически сохраняет данные в файл `data.jsonl` и обновляет графики в папке `plots`.

---

## 📡 Использование метеостанции

### Локальный режим
1. После настройки подключите устройство к вашей Wi-Fi сети.
2. Узнайте IP-адрес устройства (он отобразится в мониторе порта Arduino IDE или через роутер).
3. Откройте браузер и перейдите по адресу `http://<IP-адрес>/data`.
4. На странице будут отображаться текущие значения температуры, влажности, давления и высоты.

### API-режим
1. Убедитесь, что Flask-сервер запущен и доступен в сети.
2. Устройство будет отправлять данные на сервер через POST-запросы на endpoint `/data`.
3. Для просмотра последних данных:
   - Откройте `http://<IP-сервера>:5000/data` (GET-запрос).
4. Для просмотра графиков:
   - Температура: `http://<IP-сервера>:5000/plot/temperature.png`
   - Влажность: `http://<IP-сервера>:5000/plot/humidity.png`
   - Давление (гПа): `http://<IP-сервера>:5000/plot/pressure.png`
   - Давление (мм рт. ст.): `http://<IP-сервера>:5000/plot/pressure_mmHg.png`
   - Высота: `http://<IP-сервера>:5000/plot/altitude.png`

#### Пример данных (POST-запрос)
```json
{
  "temperature": 24.7,
  "humidity": 45.1,
  "pressure": 1010.2,
  "pressure_mmHg": 757.7,
  "altitude": 128.3
}
```

---

## 🛠️ Устранение неисправностей

| Проблема                              | Возможная причина                     | Решение                                                                 |
|---------------------------------------|---------------------------------------|-------------------------------------------------------------------------|
| Устройство не создает точку доступа   | Ошибка в прошивке                    | Проверьте код, убедитесь, что все библиотеки установлены, перепрошейте. |
| Нет данных от датчика BME280          | Неправильное подключение или адрес   | Проверьте соединения, убедитесь, что адрес I2C правильный (`0x76`).    |
| Не подключается к Wi-Fi               | Неверный SSID/пароль                 | Повторно настройте через [http://192.168.4.1](http://192.168.4.1).     |
| Сервер не получает данные             | Неверный URL или сервер недоступен   | Проверьте URL в настройках, убедитесь, что сервер запущен и доступен.  |
| Графики не обновляются                | Ошибка в `server.py` или библиотеках | Убедитесь, что `pandas` и `matplotlib` установлены, перезапустите сервер.|

---

## Вывод графиков (примеры)

- **Температура**:  
  ![temperature](plots/temperature.png)
- **Влажность**:  
  ![humidity](plots/humidity.png)
- **Давление (гПа)**:  
  ![pressure](plots/pressure.png)
- **Давление (мм рт. ст.)**:  
  ![pressure_mmHg](plots/pressure_mmHg.png)
- **Высота**:  
  ![altitude](plots/altitude.png)

---
